NXTUP prototype

Open index.html for a quick desktop preview. For full PWA/service-worker behavior, serve this folder through a local HTTP server (for example VS Code Live Server).

This version intentionally has no database and uses demo events around Karlsruhe. Account, saved searches, favorites and organizer tools are shown as future features only.
